#include "stdio.h"
#include "trie.hpp"

int main(int argc, char const *argv[])
{
    class Directory 
    // mkdir root,  cd root, mkdir child1, mkdir child2, cd child1, pwd, cd .., ls, rm child2, ls 
    fprintf(stdout, "nothing to di here\n");
    return 0;

    

}
